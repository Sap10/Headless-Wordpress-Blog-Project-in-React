import React from 'react';
import Posts from './Posts';


const Blogs = props => {
    return(
        <React.Fragment>
        <Posts pageId={1}/>
        </React.Fragment>
    );
}

export default Blogs;